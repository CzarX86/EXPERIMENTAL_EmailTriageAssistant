[pytest]
addopts = -q --maxfail=1 --disable-warnings --strict-markers
pythonpath = src
markers =
    contract: Contract tests that validate OpenAPI conformance
    integration: End-to-end tests with multiple components
    unit: Fast, isolated tests
filterwarnings =
    ignore::DeprecationWarning
