# Contract Test (Placeholder) — Ingest Endpoint

Intent: Validate that POST /ingest accepts source and returns 202.

Status: Failing by design (no implementation yet).

Expected:
- POST /ingest { source: m365 } -> 202 Accepted
