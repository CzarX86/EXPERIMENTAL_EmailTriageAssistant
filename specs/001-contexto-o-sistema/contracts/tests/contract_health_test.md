# Contract Test (Placeholder) — Health Endpoint

Intent: Validate that `/health` is documented and returns 200 OK.

Status: Failing by design (no implementation yet).

Expected:
- GET /health -> 200 OK
