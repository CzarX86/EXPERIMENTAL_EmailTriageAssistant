# Contracts — Local API

OpenAPI specification for the local loopback API used by the assistant. Authentication via bearer token stored in macOS Keychain. CORS disabled or loopback-only.

Endpoints cover ingestion, indexing, classification, suggestions, explainability, search, feedback, export and import.
